# PROJECT_REVIEW — 无痛英语 EnglishLite

日期：2026-08-11（v2：词库扩充到四级 + 边界规则重设计）
状态：完成，单元测试 + DOM 端到端全部通过

## v2 变更
- 词库 243 → **6656 条**：接入 qwerty-learner 开源 CET4 词表（2607 词），
  中文释义清洗生成 6400+ 条（去词性前缀/括号/被动结构/单字黑名单）
- **边界规则重设计**：原「前后都不能紧邻汉字」导致真实网页正文（连续汉字流）几乎无法匹配
  （2850 字长文本 0 候选）→ 改为「只挡 ASCII 字母/数字相邻」，防拆词由长词优先承担
  → 长文本 900 候选，1ms
- **首字索引**：词库按中文首字建索引，匹配只查文本中出现的汉字，性能 1ms
- 修复：初始 walk 用默认强度执行、storage 读回后不重扫 → 用户设置强度首次加载不生效。
  现在 storage 读回且设置变化时清空 processed 重扫
- storage.sync → storage.local（国内网络不卡初始化），popup 显示真实词条数

## 验证结果
- node 单测全过：词边界、短句匹配、长词优先、英文/数字相邻保护、重复词条、
  词库无重复、长文本性能（2850 字 1ms / 900 候选）
- DOM 端到端（http server + 浏览器）：全量替换 ✓、code 保护 ✓（live DOM 验证）、
  动态内容处理 ✓
- 词库生成脚本幂等：重复运行无新增重复

## 踩坑记录
1. **TreeWalker 游标失效**：遍历中 replaceChild 让 nextNode 跳过后续节点 → 先收集再处理
2. **self-test.html 的 chrome mock 用旧 storage.sync**：content.js 改 local 后 mock 不匹配，
   local.get 抛错被 catch → 回落默认强度 → 自测出现"随机替换"假象。mock 必须与代码同步。
   且 http 测试要加 ?v=N 绕过浏览器缓存。
3. **单字词拆词**：放宽边界后人工词库单字词（忙/累/新/贵/快/慢/旧）会拆「帮忙/积累/创新」等
   → build 脚本加单字黑名单
4. **build 脚本幂等性**：FIXED 词条进入 words.js 后，下次 build 的 existing 已含它们，
   concat(FIXED) 会重复 → 合并后统一 Map 去重
5. **初始强度 bug**：立即 walk 用默认设置 + storage 异步读回，若读回后不重扫，
   用户设置的强度永远不生效（自测随机性的真正根因）
6. **降强度不实时减少**（用户反馈）：调低强度只重扫新增替换、不撤销已替换 span →
   restoreAll() 先把 span 恢复成中文（span 存 data-zh 原文，title 可能因悬停提示关闭而缺失），
   再按新强度重扫；开关关闭同样全恢复。120ms 防抖避免滑块拖动频繁全量重建。
   验证：自测页阶段2 模拟强度 1.0→0.1，span 5→1 PASS

## 未做
- 词库分类/用户自定义、按站点开关、TTS 发音、生词本、Chrome Web Store 上架
